# -*- coding: utf-8 -*-
"""
from gluon.scheduler import Scheduler

def absent_count():
    return dict()

def mail_after_six_days():
    return dict()

def mail_or_sms_after_eight_days():
    return dict()

absent_count = Scheduler(db, tasks=dict(absent_count=absent_count))
mail_after_six_days = Scheduler(db, tasks=dict(mail_after_six_days=mail_after_six_days))
mail_or_sms_after_eight_days = Scheduler(db, tasks=dict(mail_or_sms_after_eight_days=mail_or_sms_after_eight_days))
"""
